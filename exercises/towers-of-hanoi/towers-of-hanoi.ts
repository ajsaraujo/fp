/**
 * Let `towers` be a variable of the type `HanoiTowers`.
 *
 * Each property represents a tower. The i-th number in each
 * array represents the size of the i-th disk in that tower from top
 * to bottom.
 */
type HanoiTowers = { left: number[]; middle: number[]; right: number[] };

function solve(towers: HanoiTowers) {}
